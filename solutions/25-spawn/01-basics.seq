: test-basics ( -- )
    chan.make
    [ 42 swap chan.send drop ] strand.spawn
    drop                  # drop strand-id, leaving ( chan )
    chan.receive drop     # receive value, drop the success flag
    42 test.assert-eq
;
