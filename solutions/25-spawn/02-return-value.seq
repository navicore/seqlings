: compute-in-strand ( -- Int )
    chan.make
    dup [ 10 5 i.* swap chan.send drop ] strand.spawn
    drop
    chan.receive drop   # (channel value)
    nip                 # (value)
;

: test-return-value ( -- )
    compute-in-strand
    50 test.assert-eq
;
