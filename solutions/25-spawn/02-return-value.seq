: compute-in-strand ( -- Int )
    chan.make
    [ 10 5 i.* swap chan.send drop ] strand.spawn
    drop                  # drop strand-id
    chan.receive drop     # ( 50 )
;

: test-return-value ( -- )
    compute-in-strand
    50 test.assert-eq
;
