: home-file ( String -- String )
    os.home-dir
    [ ]
    [ drop "/tmp" ]
    if
    swap os.path-join
;

: test-contains-name ( -- )
    "app.toml" home-file
    "app.toml" string.contains
    test.assert
;
