union Option { Some { value: Int }, None }

: is-some? ( Option -- Int )
    variant.tag 0 =
;

: is-none? ( Option -- Int )
    variant.tag 1 =
;

: test-some ( -- )
    100 Make-Some is-some?
    test.assert
;

: test-none ( -- )
    Make-None is-none?
    test.assert
;

: test-some-not-none ( -- )
    42 Make-Some is-none?
    not test.assert
;
