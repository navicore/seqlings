: count-a-word-chars ( Variant -- Int )
    [ 0 string.char-at 97 = ] list.filter
    0 [ string.length i.add ] list.fold
;

: test-combine ( -- )
    "apple banana apricot cherry" " " string.split count-a-word-chars
    12 test.assert-eq   # "apple" (5) + "apricot" (7) = 12
;
