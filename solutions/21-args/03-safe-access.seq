: arg-or ( Int String -- String )
    over args.count i.<
    [ drop args.at ]
    [ nip ]
    if
;

: test-in-bounds ( -- )
    0 "FALLBACK" arg-or
    "FALLBACK" string.equal? not test.assert
;

: test-out-of-bounds ( -- )
    99 "FALLBACK" arg-or
    "FALLBACK" string.equal? test.assert
;
