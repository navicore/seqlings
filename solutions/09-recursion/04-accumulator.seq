: sum-to-acc ( Int Int -- Int )
    over 0 i.<= if
        nip
    else
        over i.+
        swap 1 i.- swap
        sum-to-acc
    then
;

: sum-to ( Int -- Int )
    0 sum-to-acc
;

: test-accumulator ( -- )
    100 sum-to
    5050 test.assert-eq
;
