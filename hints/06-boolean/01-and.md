# Hint: Boolean AND

`and` requires both inputs to be true.

## Solution

```seq
true true and
```

## AND as a Gate

In hardware, AND gates are used to require multiple conditions. A door unlocks only if you have the key AND the code. `and` expresses this requirement in code.
