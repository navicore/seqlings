# Hint: Map Basics

## Creating and Setting

```seq
map.make                    # Create empty map
"fruit" "apple" map.set     # Add key-value pair
```

## Solution

```seq
map.make "fruit" "apple" map.set
```

The `map.set` operation takes a map, key, and value, then returns the updated map.
