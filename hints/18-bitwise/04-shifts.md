# Hint: shl and shr

Both shifts take the value first, then the shift count.

`shl` by N is the same as multiplying by 2^N. So `5 3 shl` is 5 × 8.

`shr` by N is integer division by 2^N. So `40 3 shr` is 40 ÷ 8.
